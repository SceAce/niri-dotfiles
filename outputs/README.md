# Output Profiles

This directory stores reusable `niri` output layout profiles.

The install script can materialize one of them into `~/.config/niri/output.kdl`.

Current profiles:

- `current-machine.kdl`
- `laptop-only.example.kdl`
